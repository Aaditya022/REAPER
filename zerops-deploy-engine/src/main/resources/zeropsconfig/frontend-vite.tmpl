  - setup: frontend
    build:
      base: nodejs@22
      buildCommands:
        - cd frontend && npm ci
        - cd frontend && npm run build
      deployFiles:
        - frontend/dist/~
      cache:
        - node_modules
    run:
      base: static
